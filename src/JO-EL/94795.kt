import java.util.*
fun main() {
    // Definir escaner
    val scan = Scanner(System.`in`)
    // Escanear numero
    var numero:Int = scan.nextInt()
    // Imprimir numero multiplicado por tres
    println(numero*3)
}