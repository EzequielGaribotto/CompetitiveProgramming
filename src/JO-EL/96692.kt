import java.util.*
fun main() {
    //Definir el escaner
    val scan = Scanner(System.`in`)
    //Escanear la variable costado
    var costat:Int = scan.nextInt()
    //Calcular el area del cuadrado
    var quadrat = costat*costat
    //Mostrar el resultado por pantalla
    println(quadrat)
}